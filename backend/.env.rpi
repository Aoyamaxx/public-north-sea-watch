# Django Configuration
DEBUG=False
SECRET_KEY=django-insecure-rpi-key-replace-this-with-actual-secret
DJANGO_SETTINGS_MODULE=apps.config.settings

# Database Configuration
DATABASE_URL=postgres://aoyamaxx:aoyamaxx@db:5432/backend
AIS_DATABASE_URL=postgres://aoyamaxx:aoyamaxx@db:5432/ais_data_collection

# PostgreSQL Configuration
POSTGRES_USER=aoyamaxx
POSTGRES_PASSWORD=aoyamaxx
POSTGRES_DB=backend

# Host Configuration
ALLOWED_HOSTS=aoyamaxx.com,api.aoyamaxx.com,*.aoyamaxx.com,localhost,127.0.0.1

# API Configuration
API_VERSION=v1

# Logging Configuration
LOG_LEVEL=INFO

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://aoyamaxx.com,https://www.aoyamaxx.com,http://localhost:3000 